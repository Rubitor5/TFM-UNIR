apiVersion: v1
kind: Service

metadata:
  name: project-microservice
  namespace: {{ .Values.namespace }}

spec:
  selector:
    app: project-microservice

  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: 8081

  type: ClusterIP
